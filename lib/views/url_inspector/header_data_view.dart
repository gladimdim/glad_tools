import 'package:flutter/material.dart';

class HeaderDataView extends StatelessWidget {
  const HeaderDataView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
